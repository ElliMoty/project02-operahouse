{
  "name": "project02-operahouse",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd client && npm i && npm run build",
    "start": "harp server client/build --port $PORT"
  },
  "dependencies": {
    "harp": "*"
  }
}
