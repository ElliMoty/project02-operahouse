<%= form_with(model: event_space, local: true) do |form| %>
  <% if event_space.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_space.errors.count, "error") %> prohibited this event_space from being saved:</h2>

      <ul>
      <% event_space.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_area :location %>
  </div>

  <div class="field">
    <%= form.label :capacity %>
    <%= form.number_field :capacity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
