<%= form_with(model: seat, local: true) do |form| %>
  <% if seat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(seat.errors.count, "error") %> prohibited this seat from being saved:</h2>

      <ul>
      <% seat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :seat_name %>
    <%= form.text_area :seat_name %>
  </div>

  <div class="field">
    <%= form.label :seat_num %>
    <%= form.number_field :seat_num %>
  </div>

  <div class="field">
    <%= form.label :event_space_id %>
    <%= form.select :event_space_id, EventSpace.pluck(:name, :id), :include_blank => true %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.select :user_id, User.pluck(:name, :id), :include_blank => true %> 
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
